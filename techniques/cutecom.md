## the cutecom installed from ubuntu's apt does not support baudrate higher than certain level, build from source instead.

## to locally build just apt install libqt5serialport5-dev qtbase5-dev is fine
